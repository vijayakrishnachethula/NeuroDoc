from setuptools import setup
import os

with open('requirements.txt') as f:
    required = f.read().splitlines()

setup(
    name='Converso',
    version='1.0.0',
    packages=[''],
    install_requires=[required],
    url='',
    license='',
    author='c vijaya krishna',
    author_email='',
    description=''
)
